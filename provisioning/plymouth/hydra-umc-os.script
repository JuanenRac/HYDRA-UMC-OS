# HYDRA-UMC-OS Plymouth theme: center the supplied artwork.
image = Image("splash.png");
sprite = Sprite(image);
sprite.SetX(Window.GetWidth() / 2 - image.GetWidth() / 2);
sprite.SetY(Window.GetHeight() / 2 - image.GetHeight() / 2);

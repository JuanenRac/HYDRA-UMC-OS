# Image builder

HYDRA-UMC-OS starts from an official Raspberry Pi OS ARM64 image. This folder
will contain reproducible profile manifests and build automation; it does not
produce or distribute a replacement operating system image.

The first executable deliverable is the `hydra-umc-platform-base` package and
its read-only diagnostics agent. Image assembly remains intentionally outside
this first safety-focused delivery.
